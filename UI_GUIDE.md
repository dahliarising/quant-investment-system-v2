# UI 가이드

## 🎨 최고급 웹 대시보드

### 특징
- ✨ **Glassmorphism 디자인**: 현대적인 반투명 유리 효과
- 🎭 **애니메이션**: 부드러운 페이드인, 호버 효과
- 📊 **인터랙티브 차트**: Plotly.js로 구현된 고품질 차트
- 🎯 **반응형**: 모바일, 태블릿, 데스크톱 대응
- 🌈 **그라데이션**: 아름다운 색상 조합
- ⚡ **빠른 로딩**: CDN 기반 라이브러리

### 사용 방법

#### 1. 프로젝트 루트에서 바로 실행 (권장)
```bash
./run_dashboard.sh
# 브라우저에서 http://localhost:8000/dashboard.html 접속
```

#### 2. 브라우저에서 직접 열기
```bash
# 다운로드 받은 dashboard.html 파일을 더블클릭
# 또는 브라우저로 드래그 앤 드롭
```

#### 3. 수동으로 로컬 서버 실행
```bash
cd ui
python -m http.server 8000
# 브라우저에서 http://localhost:8000/dashboard.html 접속
```

### 포함된 기능

#### 📊 대시보드
- 4개 주요 메트릭 카드 (Total Return, Sharpe Ratio, Max Drawdown, Win Rate)
- 포트폴리오 성과 차트 (Portfolio vs Benchmark)
- 섹터 분포 파이 차트
- 리스크 메트릭 바 차트

#### 🎯 포트폴리오
- Top Holdings 테이블
- 가중치 바 차트
- 실시간 상태 배지

#### 📈 팩터 분석
- 팩터 기여도 차트
- 섹터 배분 분석

#### 🎨 디자인 요소

**색상 팔레트:**
- Primary: #6366f1 (Indigo)
- Secondary: #8b5cf6 (Purple)
- Success: #10b981 (Green)
- Danger: #ef4444 (Red)
- Warning: #f59e0b (Amber)

**폰트:**
- Inter (Google Fonts)
- Font Awesome Icons

**효과:**
- Box Shadow: 다층 그림자
- Backdrop Blur: 배경 흐림 효과
- Gradient: 선형 그라데이션
- Transform: 호버 시 이동/확대

### 커스터마이징

#### 색상 변경
```css
:root {
    --primary: #6366f1;  /* 여기를 수정 */
    --secondary: #8b5cf6;
    /* ... */
}
```

#### 차트 데이터 변경
```javascript
// dashboard.html 하단의 JavaScript 섹션에서
const holdings = [
    { symbol: 'YOUR_SYMBOL', ... },
    // 데이터 추가/수정
];
```

#### 실시간 데이터 연동
```javascript
// API 엔드포인트에서 데이터 가져오기
async function updateData() {
    const response = await fetch('/api/portfolio');
    const data = await response.json();
    // 차트 업데이트
}

setInterval(updateData, 5000); // 5초마다 갱신
```

### 반응형 브레이크포인트

- Desktop: > 1024px
- Tablet: 641px ~ 1024px
- Mobile: < 640px

### 브라우저 호환성

✅ Chrome 90+
✅ Firefox 88+
✅ Safari 14+
✅ Edge 90+

### 성능 최적화

1. **CDN 사용**: 외부 라이브러리 빠른 로딩
2. **최소 DOM 조작**: 효율적인 렌더링
3. **CSS 애니메이션**: GPU 가속
4. **Lazy Loading**: 필요할 때만 로드

### 향후 계획

- [ ] 다크 모드
- [ ] 드래그 앤 드롭 위젯
- [ ] 실시간 WebSocket 연동
- [ ] 차트 확대/축소
- [ ] PDF/이미지 내보내기
- [ ] 다국어 지원

### 문제 해결

**차트가 보이지 않는 경우:**
- 인터넷 연결 확인 (CDN 라이브러리 로드 필요)
- 브라우저 콘솔에서 에러 확인

**레이아웃이 깨지는 경우:**
- 브라우저 캐시 삭제
- 최신 브라우저 사용

**터미널에 텍스트 로그만 나오는 경우:**
- `python examples/comprehensive_demo.py`는 콘솔 데모이므로 정상 동작
- 웹 UI는 `./run_dashboard.sh` 또는 `ui/dashboard.html`로 실행

**성능이 느린 경우:**
- 차트 데이터 포인트 수 줄이기
- 애니메이션 비활성화

---

💎 **Pro Tip**: 실제 프로덕션에서는 React, Vue, Svelte 등의 프레임워크로 
재구현하면 더욱 강력한 인터랙티브 기능을 추가할 수 있습니다!
